apiVersion: apps/v1
kind: Deployment
metadata:
  name: broken-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: frontend #Wrong match labels
  template:
    metadata:
      labels:
        app: backend #Wrong match labels
    spec:
      containers:
      - name: nginx-container
        image: nginx